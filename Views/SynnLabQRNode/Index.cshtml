@model IEnumerable<SAMS.Models.HandheldScannerNodeModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SerialNumberMod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Room)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SerialNumberMod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Room.RoomNumberMod)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ScannerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ScannerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ScannerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
