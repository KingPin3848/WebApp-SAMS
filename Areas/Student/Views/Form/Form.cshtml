@* @model SAMS.Areas.Student.Controllers.FormController.InputModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Form";
}

<h1>Form</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group pb-2">
                <label asp-for="courseiDs" class="control-label"></label>
                <input type="text" class="form-control" id="searchInput" placeholder="Search for courses">
                <select id="courseSelect" asp-for="courseiDs" class="form-control" asp-items="ViewBag.courseNames"></select>
                <span asp-validation-for="courseiDs" class="text-danger"></span>
            </div>
            <div class="form-group pb-2">
                <label class="control-label">Student Courses</label>
                <select asp-for="courseiDs" class="form-control" asp-items="ViewBag.studentcourses"></select>
                <span asp-validation-for="courseiDs" class="text-danger"></span>
            </div>
            <div class="form-group pt-1">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-area="" asp-controller="Home" asp-action="Index">Back to List</a>
</div>

<script>
    $(document).ready(function () {
        $("#searchInput").on("keyup", function () {
            var value = $(this).val().toLowerCase();
            $("#courseSelect option").filter(function () {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });
</script> *@